#include <stdio.h>

/**
 * main - for alphaBET
 *
 * Return: Always 0
 */
int main(void)
{
	char i;
	for (i = 'a' ; <= 'z' ; i++)
		putchar(i);
	for (i = 'A' ; <= 'Z' ; i++)
		putchar(i);
	putchar('\n')
	return (0);
}
